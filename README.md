# fs2-grpc-example

An example project of [fs2-grpc](https://github.com/typelevel/fs2-grpc).

Run server, 

```shell
sbt server/run
```

and run client in another console.

```shell
sbt client/run
```
